[project]
name = "transcription-demo-infra"
version = "0.1.0"
description = "DevOps / IaC for transcription-demo (CloudFormation: S3, Lambda, Bedrock)."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.uv]
dev-dependencies = []
